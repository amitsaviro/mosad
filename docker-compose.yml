services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: mosad
      POSTGRES_PASSWORD: mosad_dev_password
      POSTGRES_DB: mosad
    ports:
      - "5432:5432"
    volumes:
      - mosad_pg_data:/var/lib/postgresql/data

volumes:
  mosad_pg_data:
