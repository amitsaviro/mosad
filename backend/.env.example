# Copy this file to ".env" (which is gitignored) and adjust as needed.
# config.py reads these values at startup.
DATABASE_URL=postgresql+psycopg://mosad:mosad_dev_password@localhost:5432/mosad
JWT_SECRET=change-me-in-real-deployment
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440
# Optional -- powers the AI scheduling agent's real reasoning step
# (LangGraph + Claude). Without it, that feature still works, just
# falls back to a plain ratings-based heuristic instead of real AI.
ANTHROPIC_API_KEY=
